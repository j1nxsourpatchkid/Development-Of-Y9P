@bring /io.standard/

entry main()
{
    string name = input.read("Enter your name: ");
    display.show(name);

    mut num age = 0;
    display.show("Enter your age: ");
    input.read(age);
    display.show(age);

    return 0;
}
